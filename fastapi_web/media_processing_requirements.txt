# Additional requirements for media processing functionality
# Add these to your existing requirements.txt

# Core image/video processing
opencv-python==4.8.1.78
Pillow==10.0.1
ffmpeg-python==0.2.0

# Machine learning for content analysis (optional)
face-recognition==1.3.0
ultralytics==8.0.196

# Scientific computing
numpy==1.24.3
scikit-learn==1.3.0

# Additional FastAPI/async support
aiofiles==23.1.0

# For handling base64 and file operations
python-multipart==0.0.6

# Note: Some packages may require system dependencies:
# - opencv-python may need additional system libraries
# - face-recognition requires dlib which needs cmake and visual studio build tools on Windows
# - ffmpeg-python requires ffmpeg to be installed on the system
#
# Installation commands for Windows:
# pip install cmake
# pip install dlib  (may need Visual Studio Build Tools)
# 
# For ffmpeg on Windows:
# Download from https://ffmpeg.org/ and add to PATH
# Or use: winget install ffmpeg
#
# Optional GPU acceleration (if CUDA available):
# opencv-contrib-python==4.8.1.78  # Instead of opencv-python
# torch>=2.0.0  # For potential future ML models